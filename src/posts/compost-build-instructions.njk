---
layout: base.njk
title: How to Build a Compost System | Eco Mosaics Restoration
description: Step-by-step instructions for building a compost system from Eco Mosaics Restoration. Practical land stewardship for Bay Area properties.
permalink: /posts/compost-build-instructions.html
---

<style>
  .post-header {
    padding: 64px 0 40px;
    border-bottom: 1px solid var(--mist);
  }
  .post-header .label-tag { margin-bottom: 14px; }
  .post-header h1 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    line-height: 1.15;
    color: var(--bark);
    margin-bottom: 10px;
  }
  .post-header .post-meta {
    font-size: 1rem;
    color: #8a7a6a;
    letter-spacing: 0.06em;
  }
  .post-body {
    max-width: 720px;
    margin: 0 auto;
    padding: 56px 24px 96px;
  }
  .post-body h1, .post-body h2 {
    font-family: 'Playfair Display', serif;
    color: var(--bark);
    line-height: 1.2;
    margin-top: 2.4em;
    margin-bottom: 0.6em;
  }
  .post-body h1 { font-size: 1.8rem; }
  .post-body h2 {
    font-size: 1.4rem;
    padding-bottom: 10px;
    border-bottom: 1px solid var(--mist);
  }
  .post-body h3 {
    font-size: 1rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--water);
    margin-top: 2em;
    margin-bottom: 0.8em;
  }
  .post-body p {
    font-size: 1rem;
    line-height: 1.85;
    color: #4a3a2a;
    margin-bottom: 1em;
  }
  .post-body strong { color: var(--bark); font-weight: 600; }
  .post-body em { font-style: italic; color: #6a5a4a; }
  .post-body hr {
    border: none;
    border-top: 1px solid var(--mist);
    margin: 2em 0;
  }
  .post-body table {
    width: 100%;
    border-collapse: collapse;
    font-size: 1rem;
    margin: 1.4em 0;
  }
  .post-body th {
    text-align: left;
    font-size: 1rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--water);
    padding: 8px 12px;
    border-bottom: 2px solid var(--mist);
  }
  .post-body td {
    padding: 8px 12px;
    border-bottom: 1px solid var(--mist);
    color: #4a3a2a;
    line-height: 1.5;
  }
  .post-body tr:last-child td { border-bottom: none; }
  .post-body tbody tr:hover { background: var(--pale); }
  .post-body img {
    width: 100%;
    border-radius: var(--radius);
    margin: 1.6em 0;
    border: 1px solid var(--mist);
  }
  .post-body code {
    font-family: 'DM Mono', monospace;
    font-size: 1em;
    background: var(--pale);
    padding: 2px 6px;
    border-radius: 4px;
    color: var(--moss);
  }
  .post-body .check-row td:first-child { color: var(--moss); }
  @media (max-width: 600px) {
    .post-body table { font-size: 1rem; }
    .post-body th, .post-body td { padding: 6px 8px; }
  }
</style>

<div class="post-header">
  <div class="container">
    <p class="label-tag">Build Documentation</p>
    <h1>Compost System — Build Instructions</h1>
    <p class="post-meta">12-bin system · 237" × 84" × 44.5"</p>
  </div>
</div>

<div class="container">
  <div class="post-body" id="content">
    <p style="color: var(--water); font-size: 1rem;">Loading…</p>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
<script>
  fetch('/posts/compost-build-instructions.md')
    .then(r => {
      if (!r.ok) throw new Error('Could not load file');
      return r.text();
    })
    .then(text => {
      document.getElementById('content').innerHTML = marked.parse(text);
    })
    .catch(() => {
      document.getElementById('content').innerHTML =
        '<p style="color:#c0504a;">Failed to load build instructions. Make sure you are serving this over HTTP, not opening the file directly.</p>';
    });
</script>
