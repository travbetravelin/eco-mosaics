{
  "name": "eco-mosaics",
  "version": "1.0.0",
  "description": "Eco Mosaics Restoration static site",
  "scripts": {
    "build": "eleventy",
    "start": "pkill -9 -f eleventy 2>/dev/null; sleep 2 && rm -rf _site && eleventy --serve --port 8080"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
